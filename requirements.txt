# Institutional Trading Framework v2.0 - Python dependencies
# Python 3.10+ required (see blueprint Part 10).

# --- Core data / numerics ---
pandas>=2.0
numpy>=1.24

# --- Charts (dark-theme dashboards + deep-dive plots) ---
matplotlib>=3.7

# --- Downloads ---
requests>=2.31          # NSE endpoints (smart_data_fetcher A2-A11)
yfinance>=0.2.28        # Intermarket + backtest data (A12)

# --- Excel / Sheets export + alerts ---
openpyxl>=3.1
gspread>=5.10           # Google Sheets (alerts G2) - optional
python-telegram-bot>=20.4   # Telegram (alerts G1) - optional

# --- Stats ---
scipy>=1.11

# --- ML phase (collector runs now; predictor parked until ML_ENABLED) ---
scikit-learn>=1.3
xgboost>=1.7            # ML phase only
